import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.nuxtcapacitortodo',
  appName: 'Nuxt Capacitor Todo',
  webDir: '.output/public'
};

export default config;
